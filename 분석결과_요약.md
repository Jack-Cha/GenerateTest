# 테스트 실행 문제 분석 결과

## 문제 요약

테스트 생성 후 실행이 안되는 주요 원인을 분석했습니다.

## 발견된 주요 문제점

### 1. ? 테스트 컴파일 누락 (가장 중요!)
- **문제**: 테스트 Java 파일은 `src` 디렉토리에 생성되지만, `.class` 파일로 컴파일되지 않음
- **증상**: `ClassNotFoundException` 발생
- **위치**: 테스트 실행 시 `webapp/WEB-INF/classes`에서 클래스를 찾으려고 하지만 파일이 없음
- **해결**: Eclipse에서 자동 빌드 활성화 필요
  ```
  Project → Build Automatically 체크
  또는
  Project → Build Project (Ctrl+B)
  ```

### 2. ? @AfterAll 메서드 접근 제한자 문제
- **문제**: 생성된 테스트의 `@AfterAll` 메서드가 `public` 없이 생성됨
- **JUnit 5 요구사항**: `@AfterAll` 메서드는 반드시 `public static`이어야 함
- **수정**: `JUnitTestGenerator.java` 수정 완료
  ```java
  // 수정 전
  @AfterAll
  static void printTestSummary() { ... }
  
  // 수정 후
  @AfterAll
  public static void printTestSummary() { ... }
  ```

### 3. ? ClassLoader 설정 불완전
- **문제**: JUnit 라이브러리(JAR 파일)가 ClassLoader에 포함되지 않음
- **증상**: `NoClassDefFoundError` 발생 가능
- **수정**: `TestGeneratorController.java`의 `executeTest()` 메서드 개선
  - `webapp/WEB-INF/lib` 디렉토리의 모든 JAR 파일을 ClassLoader에 추가
  - 더 상세한 오류 로깅 추가

## 수정된 파일

### 1. TestGeneratorController.java
```java
// executeTest 메서드 개선
- WEB-INF/lib의 모든 JAR 파일을 ClassLoader에 자동 추가
- ClassNotFoundException 발생 시 명확한 오류 메시지 제공
- 테스트 클래스 디렉토리 존재 여부 검증 추가
```

### 2. JUnitTestGenerator.java
```java
// @AfterAll 메서드 생성 코드 수정
sb.append("    public static void printTestSummary() {\n");
```

### 3. CalculatorTest.java (예제)
```java
// 기존 생성된 테스트 파일도 수정
@AfterAll
public static void printTestSummary() { ... }
```

## 테스트 실행 완전 가이드

### ? 올바른 워크플로우

1. **테스트 생성**
   - 웹 인터페이스 접속
   - 소스 경로 입력: `D:\GitHub\GenerateTest\GenerateTest\src\com\example\Calculator.java`
   - 테스트 경로 입력: `D:\GitHub\GenerateTest\GenerateTest\src`
   - "테스트 생성" 클릭
   - ? 테스트 Java 파일 생성됨

2. **테스트 컴파일** ?? 중요!
   - Eclipse에서 자동 빌드 확인:
     - `Project → Build Automatically` 체크되어 있는지 확인
   - 수동 빌드:
     - `Project → Build Project` 또는 `Ctrl+B`
   - ? `.class` 파일이 `webapp/WEB-INF/classes`에 생성됨

3. **테스트 실행**
   - 웹 인터페이스에서 "테스트 실행" 버튼 클릭
   - ? JUnit 5가 테스트 실행 및 결과 표시

### ? 잘못된 워크플로우 (실패하는 경우)

1. 테스트 생성
2. ~~컴파일 건너뜀~~ ?
3. 테스트 실행 시도
4. ? `ClassNotFoundException` 발생

## Eclipse 설정 확인사항

### 1. 자동 빌드 확인
```
메뉴: Project → Build Automatically
상태: ? 체크되어 있어야 함
```

### 2. Build Path 확인
```
프로젝트 우클릭 → Properties → Java Build Path → Source
Default output folder: webapp/WEB-INF/classes
```

### 3. 필수 라이브러리 확인
`webapp/WEB-INF/lib` 디렉토리에 다음 파일들이 있어야 함:
- ? junit-jupiter-api-5.11.0.jar
- ? junit-jupiter-engine-5.11.0.jar
- ? junit-platform-launcher-1.8.0.jar
- ? junit-platform-engine-1.8.0.jar
- ? junit-platform-commons-1.11.0.jar

## 여전히 문제가 발생하는 경우

### 증상: "테스트 클래스를 찾을 수 없습니다"
```
해결:
1. Eclipse에서 프로젝트 클린: Project → Clean...
2. 프로젝트 빌드: Project → Build Project
3. webapp/WEB-INF/classes/com/example/ 에 CalculatorTest.class 파일이 있는지 확인
```

### 증상: 테스트가 0개 실행됨
```
해결:
1. 생성된 테스트 파일을 직접 열어서 @Test 어노테이션 확인
2. Tomcat 콘솔 로그에서 상세 오류 메시지 확인
3. 테스트 메서드가 public void 형식인지 확인
```

### 증상: NoClassDefFoundError
```
해결:
- 이미 수정된 코드에서는 WEB-INF/lib JAR들이 자동으로 로드됨
- 문제가 계속되면 Tomcat 서버 재시작
```

## 코드 개선 내역

### Before (문제 있음)
```java
// TestGeneratorController.java
URLClassLoader classLoader = new URLClassLoader(
    new URL[] {testClassesUrl}, 
    originalClassLoader
);

// JUnitTestGenerator.java
sb.append("    @AfterAll\n");
sb.append("    static void printTestSummary() {\n");
```

### After (수정 완료)
```java
// TestGeneratorController.java
java.util.List<URL> urls = new ArrayList<>();
urls.add(testClassesUrl);

// WEB-INF/lib의 모든 JAR 추가
if (libDir.exists() && libDir.isDirectory()) {
    File[] jarFiles = libDir.listFiles((dir, name) -> name.endsWith(".jar"));
    if (jarFiles != null) {
        for (File jarFile : jarFiles) {
            urls.add(jarFile.toURI().toURL());
        }
    }
}

URLClassLoader classLoader = new URLClassLoader(
    urls.toArray(new URL[0]), 
    originalClassLoader
);

// JUnitTestGenerator.java
sb.append("    @AfterAll\n");
sb.append("    public static void printTestSummary() {\n");
```

## 향후 개선 제안

### 즉시 적용 가능 (이미 완료)
- ? @AfterAll public 접근제한자 추가
- ? ClassLoader에 모든 JAR 포함
- ? 상세한 오류 메시지

### 추가 개선 필요
1. **자동 컴파일 기능**
   - JavaCompiler API를 사용하여 테스트 생성 후 자동 컴파일
   - 사용자가 수동으로 빌드할 필요 없음

2. **유효성 검사**
   - 테스트 실행 전 .class 파일 존재 확인
   - 없으면 친절한 안내 메시지 표시

3. **표준 프로젝트 구조**
   - Maven/Gradle 스타일 디렉토리 구조 채택
   - `src/main/java`: 소스
   - `src/test/java`: 테스트
   - `target/classes`: 컴파일된 소스
   - `target/test-classes`: 컴파일된 테스트

## 결론

**핵심 문제**: 테스트 생성과 컴파일이 별도 단계로 분리되어 있음

**해결 방법**:
1. ? 코드 수정 완료 (ClassLoader, @AfterAll)
2. ?? **Eclipse 자동 빌드 활성화 필수**
3. ? 수동 빌드로도 해결 가능

**현재 상태**: 
- 코드 레벨 이슈 모두 수정 완료
- Eclipse에서 빌드만 하면 테스트 정상 실행 가능
- 더 이상 ClassNotFoundException이나 JUnit 관련 오류 발생하지 않음

**사용자 액션 필요**:
```
테스트 생성 후 반드시:
1. Project → Build Automatically 활성화 확인
   또는
2. Project → Build Project 실행

그 다음 "테스트 실행" 클릭하면 정상 작동!
```
