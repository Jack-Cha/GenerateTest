# 테스트가 0개 실행되는 문제 원인 분석

## ? 핵심 문제: JUnit 라이브러리 버전 불일치

### 현재 상태
```
junit-jupiter-api-5.11.0.jar       ? (최신)
junit-jupiter-engine-5.11.0.jar    ? (최신)
junit-platform-commons-1.11.0.jar  ? (최신)
junit-platform-engine-1.8.0.jar    ? (구버전!)
junit-platform-launcher-1.8.0.jar  ? (구버전!)
```

### 문제점
- **JUnit Jupiter 5.11.0**은 **JUnit Platform 1.11.0**을 요구합니다
- 현재 Platform이 **1.8.0**으로 되어 있어 호환되지 않습니다
- 이로 인해 JUnit Launcher가 테스트 메서드를 발견하지 못합니다

## ? 해결 방법

### 방법 1: JUnit Platform을 1.11.0으로 업그레이드 (권장)

다음 파일들을 교체해야 합니다:

#### 다운로드 필요한 파일:
1. `junit-platform-engine-1.11.0.jar`
2. `junit-platform-launcher-1.11.0.jar`

#### 다운로드 링크:
- Maven Central: https://repo1.maven.org/maven2/org/junit/platform/
- 직접 링크:
  - https://repo1.maven.org/maven2/org/junit/platform/junit-platform-engine/1.11.0/junit-platform-engine-1.11.0.jar
  - https://repo1.maven.org/maven2/org/junit/platform/junit-platform-launcher/1.11.0/junit-platform-launcher-1.11.0.jar

#### 설치 순서:
```
1. 위 파일 2개를 다운로드
2. D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\lib\ 에 복사
3. 기존 1.8.0 버전 파일 삭제:
   - junit-platform-engine-1.8.0.jar (삭제)
   - junit-platform-launcher-1.8.0.jar (삭제)
4. Eclipse에서 프로젝트 클린 및 빌드
5. Tomcat 서버 재시작
```

### 방법 2: 모든 JUnit을 5.8.0으로 다운그레이드

만약 버전 업그레이드가 어려우면 모두 호환되는 버전으로 맞춥니다:

```
junit-jupiter-api-5.8.0.jar
junit-jupiter-engine-5.8.0.jar
junit-platform-commons-1.8.0.jar
junit-platform-engine-1.8.0.jar
junit-platform-launcher-1.8.0.jar
```

## ? 추가 확인 사항

### 1. ClassLoader 문제
현재 코드는 이미 개선되어 있지만, 다음을 확인해야 합니다:

```java
// ClassLoader에 JUnit 라이브러리가 포함되는지 확인
System.out.println("Loaded URLs:");
for (URL url : urls) {
    System.out.println("  - " + url);
}
```

### 2. 테스트 클래스 검증
코드에 이미 추가된 검증 로직:
- @Test 어노테이션이 있는 메서드 개수 확인
- 각 테스트 메서드 이름 출력

### 3. JUnit Engine 로드 확인
테스트 실행 시 콘솔에서 다음을 확인:
```
Creating Launcher...
Registering test execution listener...
Executing tests...
Tests found: X
```

## ? 버전 호환성 표

| JUnit Jupiter | JUnit Platform | 호환 여부 |
|--------------|----------------|-----------|
| 5.11.0       | 1.11.0         | ?        |
| 5.11.0       | 1.8.0          | ?        |
| 5.8.0        | 1.8.0          | ?        |
| 5.10.0       | 1.10.0         | ?        |

## ? 즉시 해결 방법

### 빠른 테스트 (다운로드 후)

1. **라이브러리 교체**
```cmd
cd D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\lib
del junit-platform-engine-1.8.0.jar
del junit-platform-launcher-1.8.0.jar
:: 다운로드한 1.11.0 버전 파일을 이 폴더에 복사
```

2. **Eclipse 재빌드**
```
Project → Clean...
Project → Build Project
```

3. **Tomcat 재시작**
```
서버 탭에서 Tomcat 우클릭 → Restart
```

4. **테스트 재실행**
- 웹 페이지에서 "테스트 실행" 클릭
- 이제 `tests:20, passed:20, failed:0` 같은 결과가 나와야 합니다

## ? 예상 결과

### 수정 전 (현재):
```
tests:0, passed:0, failed:0
```

### 수정 후:
```
tests:20, passed:20, failed:0  (또는 일부 실패)
Found @Test method: testCheckPermission_Case1
Found @Test method: testCheckPermission_Case2
...
Total @Test methods found: 20
Tests found: 20
Tests succeeded: 20
Tests failed: 0
```

## ? 코드 개선 사항

이미 다음 개선이 적용되었습니다:

1. ? 테스트 메서드 발견 로깅 추가
2. ? JUnit Launcher 상세 로깅
3. ? ClassLoader에 모든 JAR 포함
4. ? 더 명확한 오류 메시지

## 결론

**주요 원인**: JUnit Jupiter 5.11.0과 JUnit Platform 1.8.0의 버전 불일치

**해결책**: 
1. JUnit Platform을 1.11.0으로 업그레이드 (권장)
2. 또는 모든 JUnit을 5.8.0/1.8.0으로 통일

**작업 시간**: 5분 (다운로드 + 교체 + 재시작)

라이브러리만 교체하면 테스트가 정상적으로 실행될 것입니다!
