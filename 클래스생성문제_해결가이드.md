# ?? Project Clean 후 클래스 파일(.class)이 생성되지 않는 문제 해결 가이드

## ? 문제 현상

- Eclipse에서 `Project` > `Clean...`을 실행한 후 `webapp/WEB-INF/classes` 폴더에 `.class` 파일이 생성되지 않습니다.
- 이로 인해 Tomcat 서버를 실행하면 `ClassNotFoundException`이 발생하거나, 테스트 실행 시 클래스를 찾지 못하는 문제가 발생합니다.

## ? 핵심 원인

Eclipse의 **자동 빌드(Auto Build)** 기능이 비활성화되어 있기 때문입니다.

- **`Project Clean`의 동작**: 이 기능은 기존에 컴파일된 모든 `.class` 파일을 삭제합니다.
- **`Build Automatically`의 역할**: 소스 코드(.java)가 변경되거나 프로젝트 설정이 바뀔 때 자동으로 컴파일하여 `.class` 파일을 생성합니다.

이 옵션이 꺼져 있으면, `Clean`으로 클래스 파일이 삭제된 후 다시 생성되지 않습니다.

---

## ? 해결 방법

### 방법 1: "Build Automatically" 활성화 (가장 좋은 해결책)

이 설정을 켜두면 앞으로 소스 파일을 저장할 때마다 자동으로 컴파일됩니다.

1.  Eclipse 상단 메뉴에서 **`Project`**를 클릭합니다.
2.  **`Build Automatically`** 항목을 찾아서 **체크(?)**합니다.
    - 이미 체크되어 있다면, 체크를 해제했다가 다시 체크해주세요.

![Build Automatically](httpshttps://www.baeldung.com/wp-content/uploads/2017/06/eclipse_build_automatically.png)

이 옵션을 켜는 즉시 Eclipse가 전체 프로젝트를 빌드하기 시작하며, 잠시 후 `webapp/WEB-INF/classes` 폴더에 클래스 파일들이 생성됩니다.

### 방법 2: 수동으로 프로젝트 빌드 (임시 해결책)

자동 빌드를 사용하고 싶지 않은 경우, 필요할 때마다 직접 빌드할 수 있습니다.

1.  Eclipse 상단 메뉴에서 **`Project`**를 클릭합니다.
2.  **`Build Project`**를 클릭합니다.

이 방법을 사용하면 `Clean`을 실행한 후에는 **반드시** `Build Project`를 직접 실행해주어야 합니다.

---

## ? 확인 절차

문제가 해결되었는지 확인하는 방법입니다.

1.  **`webapp/WEB-INF/classes` 폴더 확인**:
    - Eclipse의 Project Explorer에서 해당 폴더를 새로고침(F5)합니다.
    - `com/example/Calculator.class`와 같은 `.class` 파일들이 보이는지 확인합니다.

2.  **컴파일 오류 확인**:
    - `Problems` 뷰에 컴파일 오류(빨간색 X 아이콘)가 없는지 확인합니다.
    - 오류가 있다면, 클래스 파일이 생성되지 않으므로 먼저 해결해야 합니다. (현재는 오류가 없는 것으로 확인되었습니다.)

---

## ? 추가 정보

### 왜 "Build Automatically"가 꺼져있을까?

- **기본 설정**: 일반적으로 Eclipse는 이 옵션이 기본으로 켜져 있습니다.
- **실수로 비활성화**: 사용자가 실수로 메뉴를 클릭하여 비활성화했을 수 있습니다.
- **작업 공간(Workspace) 설정**: Eclipse 작업 공간 설정에서 비활성화되어 있을 수 있습니다. 이 경우, 새 프로젝트를 만들어도 동일한 문제가 발생합니다.

### `.classpath` 파일 분석

프로젝트의 `.classpath` 파일 내용은 정상입니다.

```xml
<classpath>
    ...
    <classpathentry kind="src" path="src"/>
    <classpathentry kind="output" path="webapp/WEB-INF/classes"/>
    ...
</classpath>
```

- **`kind="src"`**: 소스 코드 폴더가 `src`로 올바르게 지정되어 있습니다.
- **`kind="output"`**: 컴파일된 클래스 파일의 출력 폴더가 `webapp/WEB-INF/classes`로 올바르게 지정되어 있습니다.

프로젝트 설정 파일 자체에는 문제가 없으므로, Eclipse IDE의 설정 문제일 가능성이 99%입니다.

## ? 최종 요약

**문제**: `Project Clean` 후 `.class` 파일이 사라지고 다시 생성되지 않음.

**원인**: Eclipse의 `Build Automatically` 옵션이 꺼져 있음.

**해결**:
1.  **`Project` > `Build Automatically`를 체크**하세요.
2.  문제가 즉시 해결되지 않으면, `Project` > `Build Project`를 수동으로 실행하세요.

이제 클래스 파일이 정상적으로 생성되고 애플리케이션을 실행할 수 있습니다.
