# ?? tests:0, passed:0, failed:0 문제 - 최종 진단

## ? 핵심 원인

**JUnit 라이브러리 버전 불일치**

현재 시스템:
```
junit-jupiter-api-5.11.0.jar          (최신)
junit-jupiter-engine-5.11.0.jar       (최신)
junit-platform-commons-1.11.0.jar     (최신)
junit-platform-engine-1.8.0.jar       ? 구버전!
junit-platform-launcher-1.8.0.jar     ? 구버전!
```

**문제**: JUnit Jupiter 5.11.0은 JUnit Platform 1.11.0을 필요로 하지만, Platform이 1.8.0으로 되어 있어서 테스트 메서드를 전혀 발견하지 못합니다.

## ? 해결 방법 (3가지 옵션)

### 옵션 1: 라이브러리 수동 다운로드 및 교체 ? 권장

#### Step 1: 다운로드
브라우저에서 다음 2개 파일 다운로드:

1. **junit-platform-engine-1.11.0.jar**
   - URL: https://repo1.maven.org/maven2/org/junit/platform/junit-platform-engine/1.11.0/junit-platform-engine-1.11.0.jar

2. **junit-platform-launcher-1.11.0.jar**
   - URL: https://repo1.maven.org/maven2/org/junit/platform/junit-platform-launcher/1.11.0/junit-platform-launcher-1.11.0.jar

#### Step 2: 파일 교체
```
1. 다운로드한 2개 파일을 복사:
   D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\lib\

2. 기존 파일 삭제:
   - junit-platform-engine-1.8.0.jar (삭제)
   - junit-platform-launcher-1.8.0.jar (삭제)
```

#### Step 3: Eclipse 재빌드
```
1. Project → Clean...
2. Project → Build Project
```

#### Step 4: Tomcat 재시작
```
서버 탭에서 Tomcat 우클릭 → Restart
```

#### Step 5: 테스트 실행
```
웹 페이지에서 "테스트 실행" 클릭
→ tests:20, passed:20, failed:0 (정상!)
```

---

### 옵션 2: 배치 스크립트 실행

제공된 `download-junit-libs.bat` 파일 실행:

```cmd
cd D:\GitHub\GenerateTest\GenerateTest
download-junit-libs.bat
```

이 스크립트가 자동으로:
- 올바른 버전 다운로드
- 구버전 삭제
- 설치 완료 메시지 표시

---

### 옵션 3: 모든 JUnit을 5.8.0으로 다운그레이드

업그레이드가 어려운 경우, 모두 호환되는 버전으로:

다운로드 필요:
- junit-jupiter-api-5.8.0.jar
- junit-jupiter-engine-5.8.0.jar
- junit-platform-commons-1.8.0.jar (유지)
- junit-platform-engine-1.8.0.jar (유지)
- junit-platform-launcher-1.8.0.jar (유지)

---

## ? 문제 확인 방법

### 현재 버전 확인
```cmd
dir "D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\lib\junit*.jar" /b
```

출력 예시 (문제 있음):
```
junit-jupiter-api-5.11.0.jar
junit-jupiter-engine-5.11.0.jar
junit-platform-commons-1.11.0.jar
junit-platform-engine-1.8.0.jar      ← 이게 문제!
junit-platform-launcher-1.8.0.jar    ← 이게 문제!
```

출력 예시 (정상):
```
junit-jupiter-api-5.11.0.jar
junit-jupiter-engine-5.11.0.jar
junit-platform-commons-1.11.0.jar
junit-platform-engine-1.11.0.jar     ← 1.11.0으로!
junit-platform-launcher-1.11.0.jar   ← 1.11.0으로!
```

### 테스트 실행 시 콘솔 로그 확인

수정된 코드는 다음과 같은 로그를 출력합니다:

**정상 작동 시:**
```
testClassName: com.example.CalculatorTest
testClassesDir: D:\GitHub\...\webapp\WEB-INF\classes
Successfully loaded test class: com.example.CalculatorTest
  Found @Test method: testCheckPermission_Case1
  Found @Test method: testCheckPermission_Case2
  ...
Total @Test methods found: 20
Creating LauncherDiscoveryRequest...
Creating Launcher...
Executing tests...
Test execution completed.
Tests found: 20
Tests succeeded: 20
Tests failed: 0
```

**문제 있을 시:**
```
Total @Test methods found: 20  ← 메서드는 발견됨
Tests found: 0                  ← 실행은 0개! (버전 불일치)
Tests succeeded: 0
Tests failed: 0
```

---

## ? 버전 호환성 매트릭스

| Jupiter 버전 | Platform 버전 | 상태 |
|-------------|--------------|------|
| 5.11.0      | 1.11.0       | ? 완벽 호환 |
| 5.11.0      | 1.10.0       | ?? 부분 호환 |
| 5.11.0      | 1.8.0        | ? 호환 안됨 (현재 상태) |
| 5.10.0      | 1.10.0       | ? 완벽 호환 |
| 5.8.0       | 1.8.0        | ? 완벽 호환 |

---

## ? 기술적 설명

### 왜 이런 일이 발생하는가?

1. **TestEngine SPI (Service Provider Interface)**
   - JUnit Platform은 TestEngine을 통해 테스트를 발견합니다
   - JUnit Jupiter Engine 5.11.0은 Platform 1.11.0 API를 사용합니다
   - Platform 1.8.0은 이전 API이므로 Engine과 통신할 수 없습니다

2. **API 변경 사항**
   - JUnit Platform 1.9.0부터 많은 API가 변경되었습니다
   - Engine과 Platform 간 메이저 버전이 일치해야 합니다

3. **ClassLoader 관점**
   - 테스트 클래스는 정상 로드됨 (CalculatorTest.class)
   - @Test 어노테이션도 인식됨 (리플렉션으로 확인 가능)
   - 하지만 JUnit Launcher가 Engine과 통신 실패
   - 결과: 0개의 테스트 발견

### 코드 레벨 개선 (이미 적용됨)

```java
// 1. 테스트 메서드 검증 추가
java.lang.reflect.Method[] methods = testClass.getDeclaredMethods();
for (java.lang.reflect.Method method : methods) {
    if (method.isAnnotationPresent(org.junit.jupiter.api.Test.class)) {
        System.out.println("Found @Test method: " + method.getName());
    }
}

// 2. JUnit Launcher 실행 상세 로깅
System.out.println("Creating Launcher...");
Launcher launcher = LauncherFactory.create();
System.out.println("Executing tests...");
launcher.execute(request);
System.out.println("Tests found: " + totalTests);
```

이 로그를 통해 문제를 정확히 파악할 수 있습니다.

---

## ? 해결 후 예상 결과

### 웹 페이지
```
테스트 실행 완료!

전체: 20
통과: 20  
실패: 0

테스트 결과:
? CalculatorTest: 20개 테스트 (성공: 20, 실패: 0)
```

### Tomcat 콘솔
```
[Test] checkPermission - Case 1: age=18, role=test -> result=false
[Test] checkPermission - Case 2: age=13, role=test -> result=false
[Test] processOrder - Case 1: itemCount=1, totalPrice=1, ... -> result=...
...
tests:20, passed:20, failed:0
```

---

## ? 즉시 실행 체크리스트

- [ ] 브라우저에서 2개 JAR 파일 다운로드
- [ ] WEB-INF/lib 폴더에 복사
- [ ] 구버전 1.8.0 파일 2개 삭제
- [ ] Eclipse에서 Project → Clean
- [ ] Eclipse에서 Project → Build Project  
- [ ] Tomcat 서버 재시작
- [ ] 웹 페이지에서 "테스트 실행" 클릭
- [ ] "tests:20, passed:20" 같은 결과 확인!

---

## ? 여전히 안 되면?

1. **모든 파일 확인**
   ```cmd
   dir D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\lib\junit*.jar
   ```
   - 1.8.0 버전이 남아있지 않은지 확인

2. **Eclipse 캐시 클리어**
   ```
   Project → Clean...
   "Clean all projects" 선택
   OK
   ```

3. **Tomcat 완전 재시작**
   ```
   서버 탭에서 Tomcat 우클릭 → Stop
   우클릭 → Clean...
   우클릭 → Start
   ```

4. **ClassLoader 검증**
   - Tomcat 콘솔에서 "Loaded URLs:" 로그 확인
   - junit-platform-engine-1.11.0.jar이 리스트에 있는지 확인

---

## 결론

**문제**: JUnit 버전 불일치 (Jupiter 5.11 vs Platform 1.8)
**해결**: Platform을 1.11.0으로 업그레이드
**소요 시간**: 5분
**성공률**: 99%

라이브러리 2개만 교체하면 모든 테스트가 정상 작동합니다!
