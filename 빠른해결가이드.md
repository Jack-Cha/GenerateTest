# 빠른 해결 가이드

## 테스트가 실행되지 않나요? 이렇게 하세요!

### ? 3단계로 해결

#### 1단계: 테스트 생성
- 웹 브라우저에서 테스트 생성 페이지 접속
- 소스 파일 경로 입력
- "테스트 생성" 버튼 클릭

#### 2단계: 프로젝트 빌드 ?? 가장 중요!
Eclipse에서 다음 중 하나를 실행:

**옵션 A: 자동 빌드 활성화** (권장)
```
메뉴: Project → Build Automatically 체크
```

**옵션 B: 수동 빌드**
```
메뉴: Project → Build Project
단축키: Ctrl + B
```

#### 3단계: 테스트 실행
- 웹 페이지로 돌아가서
- "테스트 실행" 버튼 클릭
- ? 완료!

---

## ?? 중요 체크리스트

테스트 실행 전에 반드시 확인:

```
? Eclipse 자동 빌드 활성화되어 있나요?
  → Project → Build Automatically 메뉴 확인

? 테스트 파일이 컴파일되었나요?
  → webapp/WEB-INF/classes/com/example/ 에 
     CalculatorTest.class 파일이 있는지 확인

? JUnit 라이브러리가 있나요?
  → webapp/WEB-INF/lib/ 에 
     junit-jupiter-*.jar 파일들이 있는지 확인
```

---

## ? 문제 해결

### 문제: "테스트 클래스를 찾을 수 없습니다"

**원인**: 테스트 파일이 컴파일되지 않았습니다.

**해결**:
```
1. Eclipse에서: Project → Clean...
2. 프로젝트 선택 후 OK
3. Project → Build Project
4. 다시 테스트 실행
```

### 문제: "0개의 테스트가 실행됨"

**원인**: 테스트 클래스가 로드되지 않거나 테스트 메서드가 인식되지 않음

**해결**:
```
1. 생성된 테스트 파일 열기 (예: CalculatorTest.java)
2. @Test 어노테이션이 있는지 확인
3. import org.junit.jupiter.api.Test; 가 있는지 확인
4. 테스트 메서드가 public void 형식인지 확인
```

### 문제: "java.lang.NoClassDefFoundError"

**원인**: JUnit 라이브러리가 제대로 로드되지 않음

**해결**:
```
1. Tomcat 서버 중지
2. Project → Clean...
3. 프로젝트 빌드
4. Tomcat 서버 재시작
5. 다시 테스트 실행
```

---

## ? 변경된 내용 (자동 적용됨)

이미 다음 문제들이 수정되었습니다:

? **@AfterAll 메서드 수정**
- 이제 `public static`으로 생성됩니다
- JUnit 5 규칙을 준수합니다

? **ClassLoader 개선**
- WEB-INF/lib의 모든 JAR 파일이 자동으로 포함됩니다
- 더 명확한 오류 메시지가 제공됩니다

? **오류 처리 강화**
- 테스트 클래스가 없으면 구체적인 안내 메시지 표시
- 콘솔에 상세한 디버그 정보 출력

---

## ? 팁

### Eclipse 설정 최적화

**자동 빌드 활성화** (한 번만 설정하면 됨)
```
1. Window → Preferences
2. General → Workspace
3. "Build automatically" 체크
4. Apply and Close
```

### 테스트 파일 확인 방법

**컴파일 확인**:
```
Windows 탐색기에서 확인:
D:\GitHub\GenerateTest\GenerateTest\webapp\WEB-INF\classes\com\example\

여기에 CalculatorTest.class 파일이 있어야 합니다!
```

---

## ? 핵심 요약

```
테스트 생성 → 빌드 → 테스트 실행
             ^^^^
           이 단계를 잊지 마세요!
```

**가장 흔한 실수**: 테스트를 생성한 후 빌드하지 않고 바로 실행하려고 함

**올바른 방법**: 
1. 테스트 생성
2. ? **프로젝트 빌드** ?
3. 테스트 실행

---

## ? 여전히 문제가 있나요?

1. `분석결과_요약.md` 파일을 읽어보세요 (더 자세한 설명)
2. Eclipse 콘솔 로그를 확인하세요
3. Tomcat 콘솔에서 오류 메시지를 찾아보세요

가장 많이 나오는 오류:
- `ClassNotFoundException` → 빌드 안 됨
- `NoClassDefFoundError` → 라이브러리 문제 (이미 수정됨)
- `0 tests found` → @Test 어노테이션 확인

---

**이제 테스트를 생성하고 실행해보세요!** ?
